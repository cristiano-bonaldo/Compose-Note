package cvb.com.br.composenote.feature_note.presentation.util.enum

import cvb.com.br.composenote.R

enum class EnumOrderType(val key:Int, val idString: Int) {

    ASCENDING(0, R.string.sort_type_ascending),

    DESCENDING(1, R.string.sort_type_descending)
}